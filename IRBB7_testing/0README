
The script IRBB7testing.sh contains a record of the test configurations run using the sequencing reads from Sample_IRBB7.

The query files and output directories of runs that completed have been moved to the directory ./completedruns
